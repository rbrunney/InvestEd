# Database Properties
spring.jpa.hibernate.ddl-auto=update
# Using Environment variables now rather than hardcoded values
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/portfolios
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

server.port = 8082
